## 🧩 Core Redux Toolkit concepts (very small)

You only need **4 things**:

1. **Store** – global state container
2. **Slice** – feature-based state + reducers
3. **Action** – describes “what happened”
4. **Selector / Dispatch** – read & update state

RTK hides all old Redux pain.

---

## 📦 Step 1: Install Redux Toolkit

Inside your Vite project:

```bash
npm install @reduxjs/toolkit react-redux
```

---

## 🗂 Step 2: Recommended folder structure

Inside `src/`:

```
src/
 ├─ app/
 │   └─ store.js
 ├─ features/
 │   └─ counter/
 │       └─ counterSlice.js
 ├─ main.jsx
 └─ App.jsx
```

> **Rule:** One feature = one slice

---

## 🏪 Step 3: Create the Redux Store

📄 `src/app/store.js`

```js
import { configureStore } from "@reduxjs/toolkit"
import counterReducer from "../features/counter/counterSlice"

export const store = configureStore({
  reducer: {
    counter: counterReducer
  }
})
```

### What happened?

* `configureStore` = smart store creator
* `counter` = state key
* `counterReducer` = logic for that state

---

## 🔌 Step 4: Provide store to React

📄 `src/main.jsx`

```jsx
import React from "react"
import ReactDOM from "react-dom/client"
import { Provider } from "react-redux"
import { store } from "./app/store"
import App from "./App"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Provider store={store}>
      <App />
    </Provider>
  </React.StrictMode>
)
```

👉 Now **any component** can access Redux state.

---

## ✂ Step 5: Create your first Slice

📄 `src/features/counter/counterSlice.js`

```js
import { createSlice } from "@reduxjs/toolkit"

const initialState = {
  value: 0
}

const counterSlice = createSlice({
  name: "counter",
  initialState,
  reducers: {
    increment: (state) => {
      state.value += 1
    },
    decrement: (state) => {
      state.value -= 1
    },
    incrementByAmount: (state, action) => {
      state.value += action.payload
    }
  }
})

export const { increment, decrement, incrementByAmount } = counterSlice.actions
export default counterSlice.reducer
```

### 🔥 Important magic

* You **mutate state directly** → RTK uses **Immer** behind the scenes
* No switch cases
* No action constants

---

## 🧲 Step 6: Use Redux state in a component

📄 `src/App.jsx`

```jsx
import { useSelector, useDispatch } from "react-redux"
import { increment, decrement } from "./features/counter/counterSlice"

export default function App() {
  const count = useSelector((state) => state.counter.value)
  const dispatch = useDispatch()

  return (
    <div style={{ padding: 20 }}>
      <h1>Count: {count}</h1>

      <button onClick={() => dispatch(increment())}>
        +
      </button>

      <button onClick={() => dispatch(decrement())}>
        -
      </button>
    </div>
  )
}
```

---

## 🧠 Understand this clearly (super important)

| Thing                   | Meaning                     |
| ----------------------- | --------------------------- |
| `useSelector`           | Read data from store        |
| `useDispatch`           | Send actions                |
| `dispatch(increment())` | “Hey Redux, do this change” |
| `state.counter.value`   | `counter` = store key       |

---

## ✅ What you have learned so far

✔ Redux Toolkit setup
✔ Store configuration
✔ Slice creation
✔ Reading state
✔ Updating state

This is **80% of Redux used in real projects**.

---
