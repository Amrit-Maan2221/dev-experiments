# 🧩 Phase 1 – Step 1

## **Setting up React (Industry Standard)**

We will **NOT** use Create React App.

We will use **Vite**, because that’s what modern teams use.

---

## 🚀 What is Vite? (Quick, clear)

Vite is:

* A **build tool**
* Super fast
* Uses native ES modules
* Much faster than Create React App

### Why not CRA?

* Slow
* Heavy
* Deprecated mindset
* Most companies moved on

👉 **Vite = modern React setup**

---

## 🛠️ Step-by-Step: Create React App with Vite

### 1️⃣ Open terminal

Go to the folder where you keep projects.

---

### 2️⃣ Create Vite project

Run this **exact command**:

```bash
npm create vite@latest simple-notes-app
```

You’ll see prompts.

---

### 3️⃣ Choose options (IMPORTANT)

Select exactly this:

```text
✔ Select a framework: › React
✔ Select a variant: › JavaScript
```

⚠️ **Do NOT select TypeScript**

---

### 4️⃣ Move into project

```bash
cd simple-notes-app
```

---

### 5️⃣ Install dependencies

```bash
npm install
```

---

### 6️⃣ Start dev server

```bash
npm run dev
```

You’ll see something like:

```text
Local: http://localhost:5173/
```

Open it in browser.

---

## 📁 Project Structure (Understand This)

```
simple-notes-app/
│
├─ index.html        ← ONLY html file (SPA)
├─ package.json
├─ vite.config.js
│
└─ src/
   ├─ main.jsx       ← Entry point
   ├─ App.jsx        ← Root component
   ├─ index.css
   └─ assets/
```

---

## 🧠 Very Important Files Explained

### `index.html`

```html
<div id="root"></div>
```

👉 React mounts **everything** here.

---

### `main.jsx`

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <App />
);
```

This:

* Finds `#root`
* Renders React app inside it

---

### `App.jsx`

```jsx
function App() {
  return (
    <h1>Hello React</h1>
  );
}

export default App;
```

👉 This is your **first component**

---

## 🧠 Key Takeaways (Memorize)

* React app = **One HTML file**
* UI = **Components**
* `App` is the root
* Everything renders inside `#root`
* React controls DOM, not you

---
