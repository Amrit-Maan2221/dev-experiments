# ✅ What is `useTransition`?

`useTransition` helps you **keep the UI smooth and responsive** by marking some updates as **low priority**.

---

# 🚦 Think of it like traffic lights

### 🔥 High priority (fast)

* Typing in input
* Clicking a button

### 🐢 Low priority (slow)

* Filtering 1000 items
* Rendering a big list
* Loading new page content

`useTransition` tells React:

> **“Don’t block the UI. Handle this update later.”**

---

# 🔥 Example (Most Common)

You type in search box → app filters 5000 items → UI freezes.

### Without `useTransition`

```js
const [query, setQuery] = useState("");
const [filtered, setFiltered] = useState([]);

const handleSearch = (e) => {
  setQuery(e.target.value);
  setFiltered(expensiveFilter(e.target.value));
}
```

When you type fast → **UI freezes**

---

# ✅ With `useTransition`

```js
import { useTransition, useState } from "react";

function Search() {
  const [query, setQuery] = useState("");
  const [filtered, setFiltered] = useState([]);
  const [isPending, startTransition] = useTransition();

  const handleSearch = (e) => {
    const value = e.target.value;
    setQuery(value); // high priority update

    startTransition(() => {
      setFiltered(expensiveFilter(value)); // low priority update
    });
  };

  return (
    <>
      <input value={query} onChange={handleSearch} />

      {isPending && <p>Loading...</p>}
      <List items={filtered} />
    </>
  );
}
```

---

# 🎯 Key Point

### `setQuery(value)`

✔ Immediate (high priority)
✔ Typing feels smooth

### `startTransition(() => setFiltered(...))`

✔ Low priority
✔ Runs later
✔ No UI freeze

---

# 🧠 Why does this work?

Because React can now:

* prioritize user interaction
* postpone expensive work

---

# 🟢 What does `isPending` do?

`isPending` becomes **true** while React is updating the low priority state.

You can show a loading indicator:

```js
{isPending && <p>Loading...</p>}
```

---

# ✅ Simple Explanation

### `useTransition` = “Background update mode”

* Use it when a state update is **heavy**
* Keep UI responsive
* Show loading while React updates

---

# 🚫 When NOT to use it

❌ Small apps
❌ Small list
❌ Simple state update

It’s only for **expensive rendering**.
