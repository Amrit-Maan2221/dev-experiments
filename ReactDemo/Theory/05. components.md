## **Components in React (THE FOUNDATION)**

If JSX is the *syntax*, **components are the architecture**.

---

## 1️⃣ What is a Component?

### Simple definition:

> A **component** is a **JavaScript function** that returns JSX.

That’s it. No magic.

---

### Example

```jsx
function Header() {
  return <h1>Simple Notes App</h1>;
}
```

* `Header` → function
* Returns JSX
* Used like an HTML tag

---

## 2️⃣ Why Component Names Start with CAPITAL

This is **not a style choice** — it’s a rule.

### ❌ Wrong

```jsx
function header() {
  return <h1>Notes</h1>;
}
```

React thinks:

```jsx
<header />  // HTML tag
```

---

### ✅ Correct

```jsx
function Header() {
  return <h1>Notes</h1>;
}
```

React knows:

```jsx
<Header /> // Custom component
```

📌 **Capital letter = React component**

---

## 3️⃣ How Components Work Together

### `App.jsx`

```jsx
function App() {
  return (
    <div>
      <Header />
      <NoteList />
    </div>
  );
}
```

React builds a **component tree**:

```
App
 ├─ Header
 └─ NoteList
     ├─ Note
     └─ Note
```

This is how React scales.

---

## 4️⃣ Break UI into Components (Industry Way)

Our **Simple Notes App UI**:

```
App
 ├─ Header
 ├─ NoteInput
 └─ NoteList
     └─ Note
```

Each component has **one responsibility**.

---

## 5️⃣ Hands-On: Create Components

### 1️⃣ Create folder

Inside `src`:

```
src/
 └─ components/
     ├─ Header.jsx
     ├─ NoteInput.jsx
     ├─ NoteList.jsx
     └─ Note.jsx
```

---

### 2️⃣ `Header.jsx`

```jsx
function Header() {
  return (
    <header>
      <h1>Simple Notes App</h1>
    </header>
  );
}

export default Header;
```

---

### 3️⃣ `NoteInput.jsx`

```jsx
function NoteInput() {
  return (
    <div>
      <input placeholder="Write a note..." />
      <button>Add</button>
    </div>
  );
}

export default NoteInput;
```

---

### 4️⃣ `Note.jsx`

```jsx
function Note() {
  return (
    <div>
      <p>This is a note</p>
    </div>
  );
}

export default Note;
```

---

### 5️⃣ `NoteList.jsx`

```jsx
import Note from "./Note";

function NoteList() {
  return (
    <div>
      <Note />
      <Note />
    </div>
  );
}

export default NoteList;
```

---

### 6️⃣ Update `App.jsx`

```jsx
import Header from "./components/Header";
import NoteInput from "./components/NoteInput";
import NoteList from "./components/NoteList";

function App() {
  return (
    <div>
      <Header />
      <NoteInput />
      <NoteList />
    </div>
  );
}

export default App;
```

---

## 🧠 Key Rules You Just Learned

* Components = functions
* Capital letter = component
* Components must return JSX
* Components are reusable
* UI is built as a tree

---

## 🔥 Why This Matters

Without components:

* Code becomes huge
* Hard to maintain
* Impossible to scale

With components:

* Clean structure
* Easy refactor
* Team-friendly

---
